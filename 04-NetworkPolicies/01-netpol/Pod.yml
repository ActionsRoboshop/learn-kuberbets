apiVersion: apps/v1
kind: Deployment
metadata:
  name: net-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: netpol1
  template:
    metadata:
      labels:
        app: netpol1
    spec:
      containers:
        - name: c1
          image: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: net-service
spec:
  selector:
    app: netpol1
  ports:
    - port: 80
      targetPort: 80
